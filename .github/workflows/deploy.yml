name: Deploye NexusForge Frontend

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    setps:
      - name: Checkout Code
        uses: actions/checkout@4
      
      - name: Install Node.js
        uses: actions/setup-node@v3
        with:
          node-version: "22"
      
      - name: Install Dependencies
        run: npm install
      
      - name: Build Project
        run: npm run build